{
  "name": "realguard",
  "version": "2.0.0",
  "private": true,
  "description": "RealGuard \u2014 on-device AI image detector for Chrome (MV3). No cloud, no servers: WebGPU/WASM inference + forensic signal fusion.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node --test tests/*.test.mjs",
    "calibrate": "python3 eval/calibrate.py"
  },
  "devDependencies": {
    "esbuild": "0.25.0"
  },
  "dependencies": {
    "onnxruntime-web": "1.21.0"
  }
}
